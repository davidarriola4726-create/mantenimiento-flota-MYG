rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura a las colecciones del sistema MYG
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
